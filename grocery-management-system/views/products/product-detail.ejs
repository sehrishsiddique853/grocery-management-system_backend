<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.ProductName %> - Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-3xl font-bold text-gray-800">Product Details</h1>
                <a href="/products" class="bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-md transition-colors">
                    ← Back to Products
                </a>
            </div>

            <div class="bg-white rounded-lg shadow-md p-6">
                <h2 class="text-2xl font-semibold text-gray-800 mb-4"><%= product.ProductName %></h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-gray-600">
                    <div>
                        <p><strong>Product ID:</strong></p>
                        <p class="text-sm font-mono"><%= product._id %></p>
                    </div>
                    <div>
                        <p><strong>Firm Name:</strong></p>
                        <p><%= product.FirmName %></p>
                    </div>
                    <div>
                        <p><strong>Price:</strong></p>
                        <p class="text-green-600 font-semibold">$<%= product.Price %></p>
                    </div>
                    <div>
                        <p><strong>Quantity:</strong></p>
                        <p class="<%= product.Quantity < 50 ? 'text-red-600' : 'text-gray-600' %> font-semibold">
                            <%= product.Quantity %>
                        </p>
                    </div>
                    <div>
                        <p><strong>Expiry Date:</strong></p>
                        <p class="<%= new Date(product.ExpiryDate) < new Date() ? 'text-red-600' : 'text-gray-600' %> font-semibold">
                            <%= product.ExpiryDate.toLocaleDateString() %>
                        </p>
                    </div>
                </div>
                <div class="mt-6 flex space-x-3">
                   
                    <form action="/products/delete/<%= product._id %>" method="POST" class="inline">
                        <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md transition-colors" 
                                onclick="return confirm('Are you sure you want to delete this product?')">
                            Delete Product
                        </button>
                    </form>
                    <a href="/products" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md transition-colors">
                        All Products
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>