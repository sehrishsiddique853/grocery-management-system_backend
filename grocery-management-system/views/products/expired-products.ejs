<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Expired Products</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-3xl font-bold text-gray-800">Expired Products</h1>
            <div class="flex space-x-3">
                <form action="/products/delete-expired" method="POST" class="inline">
                    <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md transition-colors"
                            onclick="return confirm('Are you sure you want to delete all expired products?')">
                        Delete All Expired
                    </button>
                </form>
                <a href="/" class="bg-gray-500 hover:bg-gray-600 text-white px-4 py-2 rounded-md transition-colors">
                    ← Back to Home
                </a>
            </div>
        </div>

        <% if (products.length > 0) { %>
            <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
                <p class="text-red-700 font-semibold">⚠️ Found <%= products.length %> expired product(s)</p>
            </div>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% products.forEach(product => { %>
                    <div class="bg-white rounded-lg shadow-md p-6 border-l-4 border-red-500">
                        <h3 class="text-xl font-semibold text-gray-800 mb-2"><%= product.ProductName %></h3>
                        <div class="space-y-2 text-gray-600">
                            <p><strong>Firm:</strong> <%= product.FirmName %></p>
                            <p><strong>Price:</strong> $<%= product.Price %></p>
                            <p><strong>Quantity:</strong> <%= product.Quantity %></p>
                            <p class="text-red-600 font-semibold">
                                <strong>Expired on:</strong> <%= product.ExpiryDate.toLocaleDateString() %>
                            </p>
                        </div>
                        <div class="mt-4 flex space-x-2">
                            <a href="/products/<%= product._id %>" class="bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded text-sm transition-colors">
                                View
                            </a>
                            <form action="/products/delete/<%= product._id %>" method="POST" class="inline">
                                <button type="submit" class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded text-sm transition-colors" 
                                        onclick="return confirm('Are you sure you want to delete this expired product?')">
                                    Delete
                                </button>
                            </form>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="bg-white rounded-lg shadow-md p-8 text-center">
                <p class="text-green-600 text-lg font-semibold">✅ No expired products found!</p>
                <p class="text-gray-600 mt-2">All products are within their expiry dates.</p>
            </div>
        <% } %>
    </div>
</body>
</html>